# Unit/integration test deps. Browser/e2e deps (playwright, ~45 MB) live in
# tests/requirements-e2e.txt — the Flask-test-client suite doesn't use them.
# pytest kept <9 to stay aligned with pytest-playwright in requirements-e2e.txt.
pytest>=8.4,<9
pytest-cov==7.0.0
pytest-flask==1.3.0
freezegun==1.5.5
