# Python
__pycache__/
venv/
.coverage
htmlcov/

# Secrets / env
.env
ops/.env
*.crt
*.key
*.pem
certs/

# User data — never commit (bcrypt hashes, API keys, TOTP secrets)
src/instance/users.db
src/instance/users.db-shm
src/instance/users.db-wal
src/instance/stations.db.bak
.perf_users.db

# PRG address-point source for the geocoder ETL (~20GB unpacked, never commit)
address_data/

# Runtime / build artifacts
flask_session/
tools/
src/static/dist/pages/*.map
tests/fixtures/snapshots/

# Logs
*.log
crash.log

# Terraform (defensive — tfstate/tfvars hold secrets in plaintext)
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Tooling
.claude/

# macOS
.DS_Store
**/.DS_Store
